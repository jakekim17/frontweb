
var ProgramMainUnionPartial = {
    OpenPopup: function (url) {
        if (url == "") {
            alert("준비된 페이지가 없습니다.");
        }
        else {
            window.open(url);
        }

        return false;
    }
};



$(document).ready(function () {

    //// tv연합형 - 파트너스정보
    //$('.box-partner-infor ul').bxSlider({
    //    minSlides: 4,
    //    maxSlides: 8,
    //    slideWidth: 120,
    //    pager: false,
    //    touchEnabled: false,
    //    auto: false // 이미지 회전이 자동으로 됨
    //});

});

