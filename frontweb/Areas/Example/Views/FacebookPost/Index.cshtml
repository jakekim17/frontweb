<script>
    // 장기 액세스 토큰 신청.
    //1. Long-lived 토큰 생성하기
       // app_id / app_scret 는 앱 설정에 있음
       // short_lived_token : https://developers.facebook.com/tools/explorer?method=GET&path={app_id}&fields%3Daccess_token&version=v2.10 에서 받는다. (Pase token은 1시간이면 만료됨)
        // 토큰 생성
        //https://graph.facebook.com/v2.10/oauth/access_token?grant_type=fb_exchange_token&client_id={app_id}&client_secret={app_secret}&fb_exchange_token={short_lived_token}
    //2. User Id 갖고 오기
        // https://graph.facebook.com/v2.2/me?access_token={long_lived_access_token}
    //3. 영구적인 Page Access Token얻기
    //https://graph.facebook.com/v2.2/{UserId}/accounts?access_token={long_lived_access_token}

    //var accessToken = '영구적인 Page Access Token 넣기';
    var accessToken = 'EAAFy00gTZB3MBAJLZB965lB3BB2HhXveZAIOtQmaZBOTJln2bJk3JWCNfIJmU1GcU5GlXvwX9VpntT42qayI0ZCo2vYAfDnpr0mzfyNo5LzyZBsDThuSBhZCkLZBbH8yj52rdZA5n4BPZCKWPln6kgrIl5GsDlvVQDrWzIeTKSyKz7RgZDZD';
    
    $.ajax({
        type: "GET",
        url: "https://graph.facebook.com/v2.10/153856151378770?fields=posts.limit(5){created_time,message,link}&access_token=" + accessToken,
        success: function (data) {
            console.log(data.posts);
        }
    });
</script>


