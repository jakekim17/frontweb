@using Wow.Tv.FrontWeb.Areas.Finance.Helpers
@using Wow.Tv.FrontWeb.Areas.Finance.Models
@model USAADRNasdaqModel
@{
    ViewBag.Title = "USAADRNasdaq";
    Layout = null;
}

<h2 class="title">나스닥 인터넷</h2>
<!-- Table -->
<div class="table-type1 row5">
    <table>
        <caption></caption>
        <colgroup>
            <col width="25%">
            <col width="25%">
            <col width="25%">
            <col width="25%">
        </colgroup>
        <thead>
            <tr>
                <th>업종명</th>
                <th>현재가($)</th>
                <th>전일대비</th>
                <th>등락률(%)</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Internet.ListData.Count != 0)
            {
                foreach (var item in Model.Internet.ListData)
                {
                    <tr>
                        <td class="text">@item.PART_NAME</td>
                        <td class="number">@Html.WorldStockMainUSAADRPrice(item.U_CHG_GUBUN, item.U_PRICE)</td>
                        <td class="number">@Html.PublishChgPriceFormat(item.U_CHG_GUBUN, item.U_NET_CHG)</td>
                        <td class="number">@Html.PublishChgRateFormat(item.U_CHG_GUBUN, item.U_NET_CHGP)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">검색된 데이터가 없습니다.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<!-- //Table -->

<h2 class="title">나스닥 네트웍크</h2>
<!-- Table -->
<div class="table-type1 row5">
    <table>
        <caption></caption>
        <colgroup>
            <col width="25%">
            <col width="25%">
            <col width="25%">
            <col width="25%">
        </colgroup>
        <thead>
            <tr>
                <th>업종명</th>
                <th>현재가($)</th>
                <th>전일대비</th>
                <th>등락률(%)</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Network.ListData.Count != 0)
            {
                foreach (var item in Model.Network.ListData)
                {
                    <tr>
                        <td class="text">@item.PART_NAME</td>
                        <td class="number">@Html.PublishPriceFormat(item.U_CHG_GUBUN, item.U_PRICE)</td>
                        <td class="number">@Html.PublishChgPriceFormat(item.U_CHG_GUBUN, item.U_NET_CHG)</td>
                        <td class="number">@Html.PublishChgRateFormat(item.U_CHG_GUBUN, item.U_NET_CHGP)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">검색된 데이터가 없습니다.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<!-- //Table -->

<h2 class="title">나스닥 통신</h2>
<!-- Table -->
<div class="table-type1 row5">
    <table>
        <caption></caption>
        <colgroup>
            <col width="25%">
            <col width="25%">
            <col width="25%">
            <col width="25%">
        </colgroup>
        <thead>
            <tr>
                <th>업종명</th>
                <th>현재가($)</th>
                <th>전일대비</th>
                <th>등락률(%)</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Comunication.ListData.Count != 0)
            {
                foreach (var item in Model.Comunication.ListData)
                {
                    <tr>
                        <td class="text">@item.PART_NAME</td>
                        <td class="number">@Html.PublishPriceFormat(item.U_CHG_GUBUN, item.U_PRICE)</td>
                        <td class="number">@Html.PublishChgPriceFormat(item.U_CHG_GUBUN, item.U_NET_CHG)</td>
                        <td class="number">@Html.PublishChgRateFormat(item.U_CHG_GUBUN, item.U_NET_CHGP)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">검색된 데이터가 없습니다.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<!-- //Table -->

<h2 class="title">나스닥 컴퓨터</h2>
<!-- Table -->
<div class="table-type1 row5">
    <table>
        <caption></caption>
        <colgroup>
            <col width="25%">
            <col width="25%">
            <col width="25%">
            <col width="25%">
        </colgroup>
        <thead>
            <tr>
                <th>업종명</th>
                <th>현재가($)</th>
                <th>전일대비</th>
                <th>등락률(%)</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Computer.ListData.Count != 0)
            {
                foreach (var item in Model.Computer.ListData)
                {
                    <tr>
                        <td class="text">@item.PART_NAME</td>
                        <td class="number">@Html.PublishPriceFormat(item.U_CHG_GUBUN, item.U_PRICE)</td>
                        <td class="number">@Html.PublishChgPriceFormat(item.U_CHG_GUBUN, item.U_NET_CHG)</td>
                        <td class="number">@Html.PublishChgRateFormat(item.U_CHG_GUBUN, item.U_NET_CHGP)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">검색된 데이터가 없습니다.</td>
                </tr>
            }
            
        </tbody>
    </table>
</div>
<!-- //Table -->

<h2 class="title">나스닥 반도체</h2>
<!-- Table -->
<div class="table-type1 row5">
    <table>
        <caption></caption>
        <colgroup>
            <col width="25%">
            <col width="25%">
            <col width="25%">
            <col width="25%">
        </colgroup>
        <thead>
            <tr>
                <th>업종명</th>
                <th>현재가($)</th>
                <th>전일대비</th>
                <th>등락률(%)</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Semiconductor.ListData.Count != 0)
            {
                foreach (var item in Model.Semiconductor.ListData)
                {
                    <tr>
                        <td class="text">@item.PART_NAME</td>
                        <td class="number">@Html.PublishPriceFormat(item.U_CHG_GUBUN, item.U_PRICE)</td>
                        <td class="number">@Html.PublishChgPriceFormat(item.U_CHG_GUBUN, item.U_NET_CHG)</td>
                        <td class="number">@Html.PublishChgRateFormat(item.U_CHG_GUBUN, item.U_NET_CHGP)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">검색된 데이터가 없습니다.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<!-- //Table -->

<h2 class="title">나스닥 바이오</h2>
<!-- Table -->
<div class="table-type1 row5">
    <table>
        <caption></caption>
        <colgroup>
            <col width="25%">
            <col width="25%">
            <col width="25%">
            <col width="25%">
        </colgroup>
        <thead>
            <tr>
                <th>업종명</th>
                <th>현재가($)</th>
                <th>전일대비</th>
                <th>등락률(%)</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Bio.ListData.Count != 0)
            {
                foreach (var item in Model.Bio.ListData)
                {
                    <tr>
                        <td class="text">@item.PART_NAME</td>
                        <td class="number">@Html.PublishPriceFormat(item.U_CHG_GUBUN, item.U_PRICE)</td>
                        <td class="number">@Html.PublishChgPriceFormat(item.U_CHG_GUBUN, item.U_NET_CHG)</td>
                        <td class="number">@Html.PublishChgRateFormat(item.U_CHG_GUBUN, item.U_NET_CHGP)</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">검색된 데이터가 없습니다.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<!-- //Table -->


